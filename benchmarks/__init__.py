"""Solver scaling benchmarks.

Not collected by the default `pytest` run: `testpaths` in `pyproject.toml` covers only
`tests`, `docs` and `README.md`, so these run only when this path is given explicitly.
See [README.md](README.md) for how to run them and what each curve means.

A package (rather than a bare directory) to match the test tree, so the modules here can
import each other by relative import.
"""
